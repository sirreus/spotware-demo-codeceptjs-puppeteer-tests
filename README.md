# spotware-demo-codeceptjs-puppeteer-tests


Download or clone repo, `cd` to project root dir.

For setup & install dependens:
- `npm i`

For run tests: 
- `npm run tests`

For more about framework read [here](https://codecept.io/helpers/Puppeteer/#puppeteer)
